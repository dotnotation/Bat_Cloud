<%= render partial: 'errors/errors', locals: {model: @bat} %>

<h1>Log New Bat</h1>

<%= form_with model: @bat do |f| %>

    <%= render "form", f: f %>

        <%= f.hidden_field :discoverer_id, value: current_researcher.id %><br>

            <%= f.submit "Log Bat", class: "btn btn-info" %>
<% end %>
