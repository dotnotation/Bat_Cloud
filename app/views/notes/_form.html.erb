<%= form_with(model: [@bat, @note]) do |f| %>
    <%= f.hidden_field :researcher_id, value: current_researcher.id %>
    <%= f.hidden_field :bat_id, value: @bat.id %>
    <%= f.text_area :content %>
    <%= f.submit :comment %>
<% end %>